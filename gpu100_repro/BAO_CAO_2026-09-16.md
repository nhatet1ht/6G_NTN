# Báo cáo 2026-09-16 — đóng thêm 2 mục [A] trong CHUA_LAM_DUOC.md

File này ghi lại 2 thử nghiệm bổ sung chạy trong ngày, tiếp nối `REPORT.md` (lượt 3) và
`CHUA_LAM_DUOC.md`. Không đụng tới các file đó — chỉ dẫn ngược lại kết quả cũ để so sánh.

**Đã chạy xong hoàn toàn** (`run_gpu100_a5_a7.sh`, 15h06p, từ 00:24 đến 15:30 cùng ngày,
**0 lỗi**):

| # | Mục đóng | Việc | Thời gian |
|---|---|---|--:|
| 1 | **A5** | Train ILCHO (sigmoid) **native** trên Starlink Phase 1-a (3.000 ep) | 8h03p |
| 2 | **A5** | Eval native Phase 1-a | 12p |
| 3 | **A5** | Train ILCHO (sigmoid) **native** trên hybrid (3.000 ep) | 3h20p |
| 4 | **A5** | Eval native hybrid | 5p |
| 5 | **A7** | Train ILCHO (sigmoid) `batch=4` (thay vì `batch=8`), 4.000 ep, Phase 2-a | 3h20p |
| 6 | **A7** | Eval batch=4 trên Phase 2-a | 4p |

Mọi siêu tham số khác giữ nguyên y hệt lượt 3 (100 agent, `N_max=27`, horizon 600s,
`learn-every=1`, `device=cuda`). Script: `run_gpu100_a5_a7.sh`, log: `run_gpu100_a5_a7.log`.

> **Lưu ý về thời gian:** giai đoạn 1 (train Phase 1-a) mất 8h03p — chậm hơn ~3× so với ước
> tính ban đầu, nhưng đã điều tra kỹ và xác nhận **đây là đặc tính thật của môi trường Phase
> 1-a ở 100 UE** (kịch bản bão hòa nặng, rất nhiều sự kiện chuyển giao thất bại phải xử lý
> thêm mỗi bước), **không phải do máy chậm** — xem chi tiết ở `START_HERE.md` mục 3. Giai đoạn
> hybrid và batch=4 chạy đúng tốc độ bình thường (~3h20p mỗi cái, khớp dự đoán).

---

## Kết quả

### A5.1 — Native training Phase 1-a, so với zero-shot (lượt 3, `REPORT.md` §7.1)

**Huấn luyện:** hội tụ ở HOF≈145 / SE≈1,12 (100 UE cố định lúc train) — cao, vì Phase 1-a ở
100 UE là kịch bản **đã biết bão hòa toàn hệ thống với mọi phương pháp** (xem `REPORT.md`
§7.1: HSNF cũng nhảy HOF lên >130 ở đó). Không có gì bất thường trong hội tụ — chỉ là trần
hiệu năng thấp do thiếu dung lượng vật lý (Phase 1-a chỉ ~11 vệ tinh khả kiến trong vùng quan
tâm).

**HOF trên Phase 1-a — native vs zero-shot**

| #UE | 5 | 20 | 40 | 70 | 100 |
|---|--:|--:|--:|--:|--:|
| Native (mới) | 0,022 | **0,172** | **0,767** | **19,04** | 130,9 |
| Zero-shot (lượt 3) | 0,022 | 0,461 | 1,989 | 24,01 | 136,2 |
| Tỉ lệ cải thiện | = | **2,7×** | **2,6×** | 1,26× | 1,04× |

**SE trên Phase 1-a — native vs zero-shot [bps/Hz]**

| #UE | 5 | 20 | 40 | 70 | 100 |
|---|--:|--:|--:|--:|--:|
| Native (mới) | 1,673 | 1,634 | 1,611 | 1,501 | 1,172 |
| Zero-shot (lượt 3) | 1,919 | 1,769 | 1,635 | 1,479 | 1,152 |

**Đọc kết quả:** train native cải thiện HOF rõ rệt ở tải vừa (20-40 UE, tốt hơn 2,6-2,7 lần),
cải thiện nhẹ ở tải cao (70 UE), gần như ngang nhau ở 100 UE (cả hai đều bão hòa, không còn
dư địa). Đánh đổi: SE thấp hơn zero-shot 8-13% ở tải thấp (5-40 UE) — chính sách native có vẻ
**thận trọng hơn** với chuyển giao (né rủi ro HOF nhiều hơn), đúng hướng bài báo kỳ vọng.

### A5.2 — Native training hybrid, so với zero-shot (lượt 3, `REPORT.md` §7.2)

**Huấn luyện:** hội tụ mượt, HOF≈12,2 / SE≈2,39 — khỏe mạnh hơn hẳn Phase 1-a (đúng như dự
đoán, vì hybrid có thêm dung lượng từ Phase 2-a).

**HOF trên hybrid — native vs zero-shot**

| #UE | 5 | 20 | 40 | 70 | 100 |
|---|--:|--:|--:|--:|--:|
| Native (mới) | 0,111 | **0,117** | **0,300** | **2,029** | 5,249 |
| Zero-shot (lượt 3) | 0,000 | 0,339 | 1,544 | 4,816 | 5,677 |
| Tỉ lệ cải thiện | (native hơi cao hơn, cả hai ~0) | **2,9×** | **5,1×** | **2,4×** | 1,08× |

**SE trên hybrid — native vs zero-shot [bps/Hz]**

| #UE | 5 | 20 | 40 | 70 | 100 |
|---|--:|--:|--:|--:|--:|
| Native (mới) | 2,475 | 2,365 | 2,361 | 2,350 | 2,382 |
| Zero-shot (lượt 3) | 2,776 | 2,687 | 2,553 | 2,490 | 2,453 |

**Đọc kết quả:** đây là cải thiện **rõ rệt nhất** trong 2 thử nghiệm A5 — HOF giảm 2,4-5,1 lần
ở tải vừa-cao (20-70 UE) khi train native thay vì zero-shot. Cùng đánh đổi SE thấp hơn 3-12%
như Phase 1-a — mẫu hình lặp lại nhất quán: **policy train native luôn thận trọng hơn (ít HOF
hơn, đổi lấy SE thấp hơn chút) so với chuyển giao zero-shot từ Phase 2-a.**

**Kết luận A5:** train native tốt hơn zero-shot ở tải vừa-cao cho cả 2 chòm vệ tinh, đặc biệt
rõ ở hybrid. Ở tải cực đại (100 UE), cả hai cách đều hội tụ về cùng mức vì bị chặn bởi giới
hạn dung lượng vật lý, không phải do chính sách. Đóng mục A5 trong `CHUA_LAM_DUOC.md`.

### A7 — batch=4 vs batch=8, Phase 2-a

**Huấn luyện:** hội tụ HOF≈17,3 / SE≈2,41 / return≈170 — **gần như giống hệt** batch=8 gốc
(HOF≈17,6 / SE≈2,41 / return≈168, xem `REPORT.md` §3).

**HOF trên Phase 2-a — batch=4 vs batch=8 (đối chiếu cả seed=0 riêng lẻ và trung bình 3-seed
từ `runs/multiseed_aggregate.md`)**

| #UE | 5 | 20 | 40 | 70 | 100 |
|---|--:|--:|--:|--:|--:|
| **batch=4 (mới)** | 0,000 | 0,128 | 1,292 | 2,386 | 4,642 |
| batch=8, seed=0 | 0,022 | 0,244 | 1,497 | 3,056 | 4,952 |
| batch=8, TB 3 seed ± std | 0,05±0,06 | 0,21±0,03 | 0,92±0,45 | 2,04±0,72 | 4,52±0,32 |

**Đọc kết quả:** mọi giá trị của `batch=4` đều nằm **trong khoảng dao động tự nhiên** đã đo
được giữa 3 lần train độc lập với `batch=8` (xem cột thứ 3) — ví dụ ở 70 UE, batch=4 cho
2,386, nằm gọn trong khoảng [1,32–2,76] của batch=8 qua 3 seed. **Không có bằng chứng cho thấy
`batch=4` tạo ra chính sách khác biệt có ý nghĩa so với `batch=8`** ở quy mô đã thử — batch
size, trong khoảng khả thi trên GPU 8GB này (4 hoặc 8), không phải là yếu tố nhạy cảm.

**Kết luận A7:** kết quả âm tính sạch — không nhạy với batch size trong khoảng đã thử được.
Vẫn chưa biết điểm `batch≥16` (bị OOM VRAM, xem A1) có khác biệt hay không. Đóng mục A7 trong
`CHUA_LAM_DUOC.md`.

---

## Kết luận chung

Cả 2 thử nghiệm đều cho tín hiệu tích cực/sạch, không có bất ngờ xấu:

1. **A5 — train native tốt hơn zero-shot**, nhất quán ở cả 2 chòm vệ tinh, rõ nhất ở tải
   vừa-cao (2,4-5,1× ít HOF hơn ở hybrid). Đây là một cải thiện thật, đáng đưa vào như một
   khuyến nghị thực hành: nếu triển khai ILCHO cho một chòm vệ tinh cụ thể, train native cho
   chòm đó tốt hơn dùng policy transfer từ chòm khác — nhất là ở tải trung bình.
2. **A7 — batch size không nhạy cảm** trong khoảng khả thi (4-8) trên phần cứng hiện có —
   kết quả âm tính nhưng sạch, củng cố thêm độ tin cậy của lượt 3 (batch=8 không phải một lựa
   chọn "may rủi" tạo ra kết quả khác biệt).
3. Cả hai đều không đổi 2 phát hiện tiêu cực đã biết (HSNF vẫn thắng throughput; baseline
   không học có `std=0` khi so lại — kiểm tra chéo pipeline vẫn đúng).

**Việc còn lại trong `CHUA_LAM_DUOC.md` sau đợt này:** chỉ còn **A1** (batch=32 đúng Table II,
cần GPU ≥16GB hoặc sửa code) và **A3** (cài đúng HSNF/LBSH theo bài gốc [22]/[26], cần đọc +
code, không phải chỉ chạy thêm) — cả hai đều không phải việc "chạy thêm thí nghiệm" đơn thuần.
Nhóm [B] (5 tham số ẩn bài báo) vẫn bị chặn cứng, cần liên hệ tác giả.
