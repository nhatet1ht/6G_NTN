# Báo cáo 2026-09-16 — đóng thêm 2 mục [A] trong CHUA_LAM_DUOC.md

File này ghi lại 2 thử nghiệm bổ sung chạy trong ngày, tiếp nối `REPORT.md` (lượt 3) và
`CHUA_LAM_DUOC.md`. Không đụng tới các file đó — kết quả tổng hợp sẽ được dẫn ngược lại từ đó
sau khi xong.

**Đang chạy nền** (`run_gpu100_a5_a7.sh`, ~9-10 giờ dự kiến, khởi động 2026-09-16 00:24):

| # | Mục đóng | Việc | Vì sao |
|---|---|---|---|
| 1 | **A5** | Train ILCHO (sigmoid) **native** trên Starlink Phase 1-a (3.000 ep) + eval | Lượt 3 chỉ có kết quả *zero-shot* (policy train trên Phase 2-a, chạy thử trên Phase 1-a) — chưa từng train riêng cho chòm vệ tinh này |
| 2 | **A5** | Train ILCHO (sigmoid) **native** trên hybrid (3.000 ep) + eval | Tương tự — hybrid trước giờ cũng chỉ zero-shot |
| 3 | **A7** | Train ILCHO (sigmoid) với `batch=4` (thay vì `batch=8` của lượt 3), `learn-every=1`, 4.000 ep, Phase 2-a + eval | Kiểm tra độ nhạy với batch size — batch=8 là điểm khả thi duy nhất đã thử do giới hạn VRAM 8GB (batch=16/32 OOM), batch=4 là điểm còn lại chưa thử trên trục này |

Mọi siêu tham số khác giữ nguyên y hệt lượt 3 (100 agent, `N_max=27`, horizon 600s, `learn-every=1`,
`device=cuda`). Script: `gpu100_repro/run_gpu100_a5_a7.sh`, log: `run_gpu100_a5_a7.log`.

*(A1 — batch=32 đúng Table II — cần GPU ≥16GB hoặc sửa code (truncated BPTT), không phải chỉ
chạy thêm nên không nằm trong đợt này. A2 phần còn lại (thêm 2 seed cho đủ 5) và A3 (cài lại
HSNF/LBSH theo bài gốc) cũng để lại cho đợt sau — xem `CHUA_LAM_DUOC.md`.)*

---

## Kết quả

*(sẽ điền khi từng giai đoạn chạy xong — xem nhật ký `START_HERE.md` để biết tiến độ trực
tiếp trong lúc chạy)*

### A5 — native training Phase 1-a

*(đang chạy)*

### A5 — native training hybrid

*(chưa chạy)*

### A7 — batch=4 vs batch=8

*(chưa chạy)*

---

## Kết luận

*(viết sau khi có đủ số liệu)*
